package kasuga.lib.core.events.client;

// NOTE: NetworkEvent.ClientCustomPayloadEvent / ServerCustomPayloadEvent were removed in
// NeoForge 1.21.1. Payload handling is now done via IPayloadHandler / PayloadRegistrar,
// so these debug hooks no longer exist.
public class PacketEvent {
}
